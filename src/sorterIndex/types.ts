export type SorterIndexEntry = {
  slug: string;
  title: string;
  description: string;
  localStoragePrefix?: string;
  deadline?: string;
  url?: string;
  iconUrl?: string;
  sourceTitle?: string;
};
